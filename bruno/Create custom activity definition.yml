info:
  name: Create custom activity definition
  type: http
  seq: 1

http:
  method: POST
  url: "{{pmBaseUrl}}/api/v1alpha1/activity-definitions"
  body:
    type: json
    data: |-
      {
        "description": "This pulls down some random user info and adds it to the activity context",
        "inputSchema": {},
        "outputSchema": {},
        "type": "user-info-activity"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
