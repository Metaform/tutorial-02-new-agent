info:
  name: Create custom activity definition
  type: http
  seq: 1

http:
  method: POST
  url: "{{pmBaseUrl}}/api/v1alpha1/activity-definitions"
  body:
    type: json
    data: |-
      {
        "description": "Say Hello World",
        "inputSchema": {},
        "outputSchema": {},
        "type": "user-info-activity"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
