info:
  name: Get All Orchestration Definitions
  type: http
  seq: 4

http:
  method: GET
  url: "{{pmBaseUrl}}/api/v1alpha1/orchestration-definitions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
